version: '3.9'

services:
  web:
    build: .
    container_name: axion_pm
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - sqlite_data:/app/data
    restart: unless-stopped

volumes:
  sqlite_data: